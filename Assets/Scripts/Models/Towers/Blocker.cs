using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Blocker : _Tower
{
    public override void Init(PlayerController pC, List<Tile> tiles)
    {
        base.Init(pC, tiles);
    }
}
